# Render service blueprint (optional) — use this as a reference for Render web service setup
services:
  - type: web
    name: cup9gpu-backend-demo
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: PORT
        value: "3000"
      - key: SERVICE_ID
        value: "srv-d6sc2nnafjfc73et5l20"